# Recogs — XcodeGen project spec. Regenerate with `make generate`.
# A multiplatform SwiftUI app over the RecogsKit framework and the local DiscogsKit package.

name: Recogs

options:
  bundleIdPrefix: com.mlkshkvch.recogs
  createIntermediateGroups: true
  developmentLanguage: en
  deploymentTarget:
    macOS: "26.0"
    iOS: "26.0"

configFiles:
  Debug: Config/Base.xcconfig
  Release: Config/Base.xcconfig

settings:
  base:
    SWIFT_VERSION: "6.0"
    MARKETING_VERSION: "1.0"
    CURRENT_PROJECT_VERSION: "1"
    CODE_SIGN_STYLE: Automatic
    ENABLE_USER_SCRIPT_SANDBOXING: YES
    ASSETCATALOG_COMPILER_GENERATE_SWIFT_ASSET_SYMBOL_EXTENSIONS: YES

packages:
  DiscogsKit:
    path: DiscogsKit

targets:
  # Everything but the @main entry point: SwiftData cache, services, and the SwiftUI feature layer.
  # Keeping the views here too means only AppServices and the root view need to be public, and the
  # tests can run unhosted — no app launch, no sandbox, no signing.
  RecogsKit:
    type: framework
    supportedDestinations: [macOS, iOS]
    sources:
      - Sources/Kit
      # UserDefaults is a required-reason API and the calls live in this framework, so its bundle
      # carries the declaration. The app bundle gets the same file; Apple scans both.
      - Config/PrivacyInfo.xcprivacy
    dependencies:
      - package: DiscogsKit
        product: DiscogsKit
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.mlkshkvch.recogs.kit
        GENERATE_INFOPLIST_FILE: YES
        DEFINES_MODULE: YES

  Recogs:
    type: application
    supportedDestinations: [macOS, iOS]
    sources:
      - Sources/App
      - Config/PrivacyInfo.xcprivacy
    dependencies:
      - target: RecogsKit
    info:
      path: Config/Info.plist
      properties:
        CFBundleDisplayName: Recogs
        CFBundleName: Recogs
        CFBundleShortVersionString: "$(MARKETING_VERSION)"
        CFBundleVersion: "$(CURRENT_PROJECT_VERSION)"
        NSHumanReadableCopyright: "© 2026 Emil Kashkevich · MIT License"
        ITSAppUsesNonExemptEncryption: false
        UILaunchScreen: {}
        # Ignored on macOS. Without these an iOS Release build warns, and the app would inherit
        # whatever the toolchain defaults to rather than what it was designed for.
        UISupportedInterfaceOrientations~iphone:
          - UIInterfaceOrientationPortrait
          - UIInterfaceOrientationLandscapeLeft
          - UIInterfaceOrientationLandscapeRight
        UISupportedInterfaceOrientations~ipad:
          - UIInterfaceOrientationPortrait
          - UIInterfaceOrientationPortraitUpsideDown
          - UIInterfaceOrientationLandscapeLeft
          - UIInterfaceOrientationLandscapeRight
    entitlements:
      path: Config/Recogs.entitlements
      properties:
        com.apple.security.app-sandbox: true
        # Discogs API and the image CDN.
        com.apple.security.network.client: true
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.mlkshkvch.recogs
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        ASSETCATALOG_COMPILER_GLOBAL_ACCENT_COLOR_NAME: AccentColor
        ENABLE_PREVIEWS: YES
        ENABLE_HARDENED_RUNTIME: YES
        # XcodeGen emits only the iOS rpath for a multiplatform target, but a macOS executable sits
        # in Contents/MacOS, one level below the embedded frameworks. Without the macOS variant the
        # app launches straight into a dyld "Library not loaded: @rpath/RecogsKit.framework" abort.
        LD_RUNPATH_SEARCH_PATHS: "$(inherited) @executable_path/Frameworks"
        LD_RUNPATH_SEARCH_PATHS[sdk=macosx*]: "$(inherited) @executable_path/../Frameworks"
        TARGETED_DEVICE_FAMILY: "1,2"
        # iOS needs application-identifier and keychain-access-groups for the Keychain, and macOS
        # rejects application-identifier outright, so the two platforms take different files.
        CODE_SIGN_ENTITLEMENTS[sdk=iphoneos*]: Config/Recogs-iOS.entitlements
        CODE_SIGN_ENTITLEMENTS[sdk=iphonesimulator*]: Config/Recogs-iOS.entitlements

  # Not app-hosted: the runner is unsandboxed and needs no signing, which matters for the
  # Keychain-backed token store.
  RecogsTests:
    type: bundle.unit-test
    supportedDestinations: [macOS, iOS]
    sources:
      - Tests
    dependencies:
      - target: RecogsKit
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.mlkshkvch.recogs.tests
        GENERATE_INFOPLIST_FILE: YES

schemes:
  Recogs:
    build:
      targets:
        RecogsKit: all
        Recogs: [run, profile, archive, analyze]
        RecogsTests: [test]
    run:
      config: Debug
    test:
      config: Debug
      targets:
        - RecogsTests
    archive:
      config: Release
